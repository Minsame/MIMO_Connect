# MIMO_Connect 运行依赖
# pip install -r requirements.txt
# 开发/测试工具在 requirements-dev.txt（普通用户不需要）

# === LLM 依赖（中间层意图路由、段改写、进度摘要、语音浓缩）===
langchain-openai>=0.0.8
langchain-core>=0.1.0

# === HTTP 客户端 ===
httpx>=0.24.0

# === 配置管理 ===
python-dotenv>=1.0.0
pyyaml>=6.0

# === MiMo TTS API (OpenAI 兼容协议) ===
openai>=1.0.0

# === Edge TTS（降级引擎）===
edge-tts>=6.1.9

# === 飞书机器人（选装：不用飞书可不装）===
lark-oapi>=1.4.0

# === 微信消息解密（选装：不用微信可不装）===
pycryptodome>=3.19.0

# === Windows TTS（选装：仅 Windows 且要 Windows TTS 才需要）===
pywin32>=306

# === GUI 桌面应用（仅 Windows 源码运行需要；Linux CLI 无需安装）===
PySide6>=6.5.0
