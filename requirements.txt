# MIMO_Connect 依赖

# LLM 依赖
langchain-openai>=0.0.8
langchain-core>=0.1.0

# HTTP 客户端（微信平台、TTS、向导联通性检测直接使用）
httpx>=0.24.0

# MiMo TTS 依赖
openai>=1.0.0          # MiMo TTS API (OpenAI 兼容协议)

# 飞书/Lark Bot
lark-oapi>=1.4.0

# 微信 iLink Bot 消息解密 (提供 Crypto 模块)
pycryptodome>=3.19.0

# 配置管理
python-dotenv>=1.0.0
pyyaml>=6.0

# Windows TTS 依赖 (选择其一)
pywin32>=306           # 方案1: win32com (推荐)
# pyttsx3>=2.90        # 方案2: pyttsx3
# comtypes>=1.4.1      # 方案3: comtypes

# Edge TTS (在线降级)
edge-tts>=6.1.9

# GUI 版（Windows 桌面）依赖；CLI 版不需要
PySide6>=6.5.0

# 类型检查
mypy>=1.0.0

# 测试依赖
pytest>=7.0.0
pytest-asyncio>=0.21.0

# 开发依赖
black>=23.0.0
ruff>=0.0.280
